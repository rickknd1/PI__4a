---
apiVersion: v1
kind: Service
metadata:
  name: voice-db
  namespace: clubhub
  labels: {app: voice-db, tier: database}
spec:
  clusterIP: None
  selector: {app: voice-db}
  ports:
    - {name: mongo, port: 27017, targetPort: 27017}
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: voice-db
  namespace: clubhub
spec:
  serviceName: voice-db
  replicas: 0
  selector: {matchLabels: {app: voice-db}}
  template:
    metadata: {labels: {app: voice-db, tier: database}}
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - {key: kubernetes.io/hostname, operator: In, values: [k8s-worker-1]}
      containers:
        - name: mongo
          image: docker.io/library/mongo:7
          ports: [{containerPort: 27017, name: mongo}]
          volumeMounts:
            - {name: data, mountPath: /data/db}
          resources:
            requests: {memory: 256Mi, cpu: 100m}
            limits: {memory: 1Gi, cpu: 1000m}
          livenessProbe:
            tcpSocket: {port: 27017}
            initialDelaySeconds: 30
      volumes:
        - name: data
          hostPath:
            path: /mnt/worker-storage/voice-db
            type: DirectoryOrCreate
