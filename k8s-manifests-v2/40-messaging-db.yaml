---
apiVersion: v1
kind: Service
metadata:
  name: messaging-db
  namespace: clubhub
  labels: {app: messaging-db, tier: database}
spec:
  clusterIP: None
  selector: {app: messaging-db}
  ports:
    - {name: mongo, port: 27017, targetPort: 27017}
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: messaging-db
  namespace: clubhub
spec:
  serviceName: messaging-db
  replicas: 1
  selector: {matchLabels: {app: messaging-db}}
  template:
    metadata: {labels: {app: messaging-db, tier: database}}
    spec:
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - {key: kubernetes.io/hostname, operator: In, values: [k8s-worker-1]}
      containers:
        - name: mongo
          image: docker.io/library/mongo:7
          ports: [{containerPort: 27017, name: mongo}]
          volumeMounts:
            - {name: data, mountPath: /data/db}
          resources:
            requests: {memory: 256Mi, cpu: 100m}
            limits: {memory: 1Gi, cpu: 1000m}
          livenessProbe:
            tcpSocket: {port: 27017}
            initialDelaySeconds: 30
          readinessProbe:
            exec:
              command: ["mongosh", "--quiet", "--eval", "db.runCommand('ping').ok"]
            initialDelaySeconds: 15
      volumes:
        - name: data
          hostPath:
            path: /mnt/worker-storage/messaging-db
            type: DirectoryOrCreate
