"""Custom ML models owned by ClubHub (scikit-learn + rule-based NLP).

Nothing here talks to an external API — all inference runs locally from
the club's own data. See `recommender.py` and `pv_builder.py`.
"""
